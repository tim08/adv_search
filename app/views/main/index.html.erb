<header id="head" style="background:#181015 url(<%= image_url('bg_header.jpg') %>) no-repeat">
  <div class="container">
    <div class="row">
      <h1 class="lead"><%= @custom_description.find_by_name('первая строка описания').try(:body) %>
      </h1>
      <h2 >
        <%= @custom_description.find_by_name('вторая строка описания').try(:body) %>
      </h2>
      <p class="tagline" style="font-size: 20px">
        <%= @custom_description.find_by_name('третья строка описания').try(:body) %>

      </p>
      <%= render 'search_form' %>
    </div>
  </div>
</header>

<%= render 'content_companies' %>

<%= render 'description_service' %>

<%= render 'specializations' %>

<%= render 'faq' %>

<%= render 'call_me'%>




