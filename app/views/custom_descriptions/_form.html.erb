<%= form_with(model: custom_description, local: true) do |form| %>
  <% if custom_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_description.errors.count, "error") %> prohibited this custom_description from being saved:</h2>

      <ul>
      <% custom_description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-6">
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :custom_description_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, id: :custom_description_body, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  </div>
</div>
<% end %>
